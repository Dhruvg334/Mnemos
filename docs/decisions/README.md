# Architecture decisions

Architecture decision records capture choices that are costly to reverse or affect multiple subsystems. Records are never deleted when superseded; a new decision links to and replaces the old one.

Each record contains status, context, decision, consequences, and alternatives considered.
