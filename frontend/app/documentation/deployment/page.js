import DocLayout from "@/components/public/DocLayout";
import { InfrastructureDiagram } from "@/components/public/Diagrams";
export const metadata={title:"Deployment and operations"};
export default function Page(){return <DocLayout eyebrow="Operations" title="Deployment is treated as part of system correctness." summary="CI validates code, migrations, runtime imports, prompts, and container builds. Production configuration rejects weak secrets, wildcard CORS, default storage credentials, and development-only authentication paths."><div className="grid gap-6"><InfrastructureDiagram/><section className="rounded-[28px] border border-line bg-paper p-6"><h2 className="text-[22px] font-semibold text-ink">Operational checks</h2><div className="mt-4 grid gap-3">{["Run Alembic migrations in a separate job before API deployment.","Use pgvector-enabled PostgreSQL in local, CI, and deployed environments.","Verify database, Redis, and object storage readiness independently from liveness.","Keep agent and ingestion services behind authenticated service contracts.","Store secrets outside Git and validate required production configuration at startup.","Retain structured logs with request, query, run, and trace identifiers."].map(x=><div key={x} className="rounded-2xl bg-paper-alt px-4 py-4 text-[13px] leading-6 text-ink-soft">{x}</div>)}</div></section></div></DocLayout>}
