import DocLayout from "@/components/public/DocLayout";
import { AgentWorkflowDiagram } from "@/components/public/Diagrams";
export const metadata={title:"Query and retrieval engine"};
export default function Page(){return <DocLayout eyebrow="Retrieval" title="Different questions require different retrieval plans." summary="Mnemos combines query classification, metadata scope, vector search, bounded graph expansion, evidence mapping, and reranking. It does not merge graph relationships and cosine scores as if they were equivalent."><div className="grid gap-6"><AgentWorkflowDiagram/><section className="rounded-[28px] border border-line bg-paper p-6"><h2 className="text-[22px] font-semibold text-ink">Strategy examples</h2><div className="mt-4 divide-y divide-line">{[["Asset query","Site + asset scope → aliases → graph neighbours → vector evidence → rerank"],["RCA query","Event history → failure relations → evidence expansion → contradictions → rerank"],["Compliance query","Requirement scope → current revision → evidence status → gap analysis"],["General query","Semantic candidates → graph-linked evidence → deduplicate → rerank"]].map(([a,b])=><div key={a} className="grid gap-2 py-4 md:grid-cols-[160px,1fr]"><div className="text-[13px] font-semibold text-ink">{a}</div><div className="font-mono text-[11.5px] leading-6 text-ink-soft">{b}</div></div>)}</div></section></div></DocLayout>}
